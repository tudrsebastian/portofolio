import Navbar from "./Navbar";

const Hero = () => {
  return (
    <div className="h-80 bg-cover bg-gradient-to-r  from-blue-700 to-slate-500">
      <Navbar />
    </div>
  );
};

export default Hero;
